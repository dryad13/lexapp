[build]
  command = "npm install && npm run build --workspace firm-portal"
  publish = "apps/firm-portal/.next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
