[build]
  command = "npm install && npm run build --workspace client-portal"
  publish = "apps/client-portal/.next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
