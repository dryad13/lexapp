[build]
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"

# Pretty URLs (optional) — uncomment if you want /eligibility-check instead
# of /eligibility-check.html once this is live.
# [[redirects]]
#   from = "/eligibility-check"
#   to = "/eligibility-check.html"
#   status = 200
